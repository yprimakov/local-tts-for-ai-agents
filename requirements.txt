kokoro-onnx
sounddevice
soundfile

# Platform-specific ONNX Runtime (setup.py selects the right one automatically):
#   Windows: onnxruntime-directml  (DirectX 12 GPU acceleration)
#   macOS:   onnxruntime            (standard build)
