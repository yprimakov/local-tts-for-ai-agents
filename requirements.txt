kokoro-onnx
onnxruntime
sounddevice
soundfile

# Note: onnxruntime-directml is NOT used. It has a known bug with Kokoro's
# ConvTranspose operation and causes DLL init hangs on some AMD/Windows systems.
# Plain onnxruntime with CPUExecutionProvider is ~6x realtime and sufficient.
